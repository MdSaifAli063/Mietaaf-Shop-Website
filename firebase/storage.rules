rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /products/{allPaths=**} {
      allow read: if true;
      // Development-friendly: require auth for uploads. Tie to admin claims in production.
      allow write: if request.auth != null;
    }
  }
}
